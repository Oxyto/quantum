# Quantum Module

This is the projects repository for the module Quantum Computing.

It contains an `uv.lock` and `*.ipynb` files.

# How to use the local env

Get `uv` on your system (via PIP or else), then `uv sync`. It will get the correct version of Python (3.14) and all the necessary packages.
